import 'package:flutter/material.dart';


class WidgetNoFound extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
       body: Center(
         child: Text('没有找到对应的界面'),
       ),
    );
  }
}
